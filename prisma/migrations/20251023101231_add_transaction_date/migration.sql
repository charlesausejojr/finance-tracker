BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[transactions] ADD [date] DATETIME2 NOT NULL CONSTRAINT [transactions_date_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
